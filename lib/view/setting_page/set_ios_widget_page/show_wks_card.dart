import 'package:flutter/material.dart';

class ShowWKSCard extends StatelessWidget {
  const ShowWKSCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [
          const Text("Show WKS"),
          const Placeholder(),
        ],
      ),
    );
  }
}
